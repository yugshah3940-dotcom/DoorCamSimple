name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Install Gradle
      run: |
        wget https://services.gradle.org/distributions/gradle-8.3-bin.zip
        unzip -qq gradle-8.3-bin.zip
        echo "$PWD/gradle-8.3/bin" >> $GITHUB_PATH

    - name: Build APK
      run: gradle -g $HOME/.gradle assembleRelease --stacktrace

    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: DoorCamSimple-APK
        path: app/build/outputs/apk/release/app-release.apk

